# Vercel Environment Variables (Frontend Only)
# Safe to import - contains no sensitive backend credentials

# API Connection (Update after deploying backend)
NEXT_PUBLIC_API_BASE=https://your-backend-url.railway.app

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=
